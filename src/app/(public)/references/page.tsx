import { PageContainer } from "@/components/layout/page-container";
import { ReferencesList } from "@/features/content/references/references-list";

export default function ReferencesRoutePage() {
  return (
    <PageContainer>
      <ReferencesList />
    </PageContainer>
  );
}

